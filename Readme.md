1.
The variable declaration provides function scope access to variables while permitting users to declare the same variable name again and change its value.
The let statement is special because it creates a block scope. This means it stops names from being declared again, but it's okay to change their values.
The const declaration establishes block scope with a restriction that prevents name re-declaration and value modification for the predefined constant.

2.
The spread operator enables you to extract all array or object elements.
The function `Math.max` converts all array elements into separate individual values.
The method enables users to create duplicate arrays and objects while providing a solution to different problems which may occur in various scenarios.

3.
The map function creates a new array which contains modified versions of its original items.
The function processes every item through its function but does not yield an output array.
The function generates a fresh array which contains only those items that satisfy the established criteria.

4.
JavaScript enables you to create functions using arrow function syntax which employs the `=>` notation for concise expression.
The example shows how you can create an arrow function using the `const add` syntax which takes two parameters.
The syntax provides a more compact method for function definition which lacks its own `this` context.

5.
Template literals are strings that use backticks to create strings which allow variables and expressions through the `${}` syntax.
Example:
The variable `name` is assigned the value `Fahim` using the statement `let name = "Fahim";`.
The system enables users to write strings that span multiple lines.